var defaultConfig = {};

defaultConfig.apiKey = 'get your server api key from the google developer console';
defaultConfig.regionCode = 'FI';
defaultConfig.maxScore = 50; // don't prefer youtube over other backends

module.exports = defaultConfig;
